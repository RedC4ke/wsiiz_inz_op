import 'package:flutter/material.dart';

class BlImagePicker extends StatelessWidget {
  const BlImagePicker({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
